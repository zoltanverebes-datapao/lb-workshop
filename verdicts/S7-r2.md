# Verdict: S7 round 2
VERDICT: FAIL

## Gate
scripts/verify.sh — stage reached: full gate (contract), exit code: 0

## Per-criterion

- [PASS] C1 — `cd backend && DATABASE_URL=postgresql://user@localhost/db uv run python -c "from app.db import get_yoyo_url; u=get_yoyo_url(); assert u.startswith('postgresql+psycopg://'), u; print('OK')"` → OK

- [PASS] C2 — `cd backend && DATABASE_URL=postgresql://x@localhost/test uv run python -c "import sys; from app.db import pool; assert 'databricks' not in sys.modules; print('OK')"` → OK

- [PASS] C3 — `cd backend && env -u DATABASE_URL -u PGHOST uv run python -c "from app.db import pool" 2>&1 | grep -q 'RuntimeError' && echo OK` → OK

- [PASS] C4 — `grep -q 'databricks-sdk' backend/pyproject.toml && echo OK` → OK

- [PASS] C5 — `! grep -qE '^from databricks|^import databricks' backend/app/db.py && grep -q 'databricks' backend/app/db.py && echo OK` → OK

- [PASS] C6 — `grep -q 'get_yoyo_url' backend/app/main.py && ! grep -q 'os.environ\["DATABASE_URL"\]' backend/app/main.py && echo OK` → OK

- [PASS] C7 — `! grep -q 'DATABASE_URL' scripts/start.sh && echo OK` → OK

- [PASS] C8 — `grep -q 'set -e' scripts/start.sh && grep -qE 'exec .+uvicorn' scripts/start.sh && grep -q '\-\-host 0.0.0.0' scripts/start.sh && grep -q 'DATABRICKS_APP_PORT' scripts/start.sh && echo OK` → OK

- [PASS] C9 — `! grep -qE 'PGHOST|PGPORT|PGDATABASE' scripts/start.sh && echo OK` → OK

- [PASS] C10 — `cd backend && DATABASE_URL=postgresql://x@localhost/test uv run python -c "from app.db import pool; assert pool.conninfo == 'postgresql://x@localhost/test', pool.conninfo; print('OK')"` → OK

- [PASS] C11 — `env -u DATABASE_URL -u PGHOST uv run python -c "from app.db import pool" 2>&1 | grep -q 'DATABASE_URL' && ... grep -q 'PGHOST' && echo OK` → OK; error message: "No database configuration found. Set either DATABASE_URL (local/test) or PGHOST (Databricks Lakebase). Neither DATABASE_URL nor PGHOST is set."

- [PASS] C12 — `bash scripts/verify.sh` → exit 0; all 12 contract tests pass, all backend/frontend unit tests pass, lint and types clean.

- [PASS] S1 — `bash scripts/verify.sh` → exit 0 ("=== gate green ===")

- [N/A] S2 — No database model changes in diff.

- [N/A] S3 — No migration required.

- [N/A] S4 — No migration required.

- [N/A] S5 — No new API routes added.

- [N/A] S6 — No test-only route changes; standing guard is unchanged.

- [PASS] S7 — `git diff | grep -nE '# type: ignore|: any|as any|ts-expect-error'` → no output; no type escape hatches introduced.

- [FAIL] S8 — Scope violated: `playwright.config.ts` is modified but is explicitly listed in the spec's Scope Out. The spec states: "Test configuration (`playwright.config.ts`, `backend/tests/conftest.py`) -- tests set `DATABASE_URL` directly" under Scope Out. The change adds `testIgnore: '**/contract-backup/**'`. This file is not in Scope In, not in the rubric-base.md exemptions (`api/alembic/versions/**`, `api/uv.lock`, `frontend/package-lock.json`, `frontend/src/api/types.ts`), and the spec explicitly placed it out of scope. `git diff --name-only` shows: `backend/app/db.py`, `backend/app/main.py`, `backend/pyproject.toml`, `backend/uv.lock`, `playwright.config.ts`, `scripts/start.sh`, `specs/S7.md`. Of these, `backend/uv.lock` is the lock-file equivalent of the exempted `api/uv.lock` and is acceptable. `specs/S7.md` is an expected status update. `playwright.config.ts` is the offending file.
       → Remove the `testIgnore` change from `playwright.config.ts`, or move it to a separate spec that covers that configuration concern. The spec says test configuration is out of scope for S7.

- [N/A] S9 — `git diff --name-only HEAD -- tests/contract/` is empty; no contract tests exist for S7, none were modified.

- [PASS] S10 — `git diff | grep -nEi 'postgres://|password\s*=|api[_-]?key'` → no output.

## Notes

Every spec criterion C1–C12 passes. The sole failure is S8 (scope): the builder modified `playwright.config.ts`, which the spec's own Scope Out section explicitly exempted. The change itself is benign (adding `testIgnore` to avoid scanning a `contract-backup` directory that presumably appeared as a side-effect of development), but scope discipline is non-negotiable — a test-configuration file the spec said was out of scope should not be touched as part of this item. The fix is to revert the `playwright.config.ts` change; if the `contract-backup` directory causes a real problem it should be handled under its own spec or as a housekeeping commit separate from S7.

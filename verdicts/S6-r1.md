# Verdict: S6 round 1
VERDICT: FAIL

## Gate
scripts/verify.sh — stage reached: contract, exit code: 1

The full gate exits non-zero. The backend, frontend, and build stages all pass
(exit 0). The contract stage fails: 59 tests fail, 21 pass. All 12 S6.spec.ts
tests pass. The 59 failures are in `tests/contract/contract-backup/` — a
subdirectory present in the workspace that Playwright picks up because
playwright.config.ts uses `testDir: '.'`. These failures pre-date S6: running
`git stash` (which has no effect because the new files are all untracked)
produces the identical 59-failure result. The contract-backup tests use
`path.resolve(__dirname, '..', '..')` which resolves to `tests/` instead of
the project root, so every file-existence check fails.

S1 requires `bash scripts/verify.sh` to exit 0. It does not. The cause is
pre-existing breakage in `contract-backup/`, not anything introduced by this
diff, but the criterion is unconditional.

## Per-criterion

### Spec-specific criteria

- [PASS] C1 — `python3 -c "import yaml, sys; d=yaml.safe_load(open('app.yaml')); assert d['command']==['bash','scripts/start.sh'], ...; print('OK')"` → `OK`
  Also confirmed by S6.spec.ts C1 test passing.

- [PASS] C2 — `test -x scripts/start.sh && echo OK` → `OK`
  File exists and is executable.

- [PASS] C3 — `head -1 scripts/start.sh | grep -qx '#!/usr/bin/env bash' && echo OK` → `OK`
  First line is exactly `#!/usr/bin/env bash`.

- [PASS] C4 — `grep -q 'set -e' scripts/start.sh && echo OK` → `OK`

- [PASS] C5 — `grep -qE 'npm (install|ci)' scripts/start.sh && echo OK` → `OK`
  Script contains `npm install`.

- [PASS] C6 — `grep -q 'npm run build' scripts/start.sh && echo OK` → `OK`

- [PASS] C7 — `grep -q 'uv sync' scripts/start.sh && echo OK` → `OK`

- [PASS] C8 — uvicorn, host, and port variable checks → `OK`
  Script contains `uvicorn app.main:app`, `--host 0.0.0.0`, and `DATABRICKS_APP_PORT`.

- [PASS] C9 — `grep -qE 'exec .+uvicorn' scripts/start.sh && echo OK` → `OK`
  Final line is `exec uv run uvicorn app.main:app --host 0.0.0.0 --port "$DATABRICKS_APP_PORT"`.

- [PASS] C10 — `! grep -qiE '(password|secret|postgres://|DATABASE_URL)' app.yaml && echo OK` → `OK`
  app.yaml contains only the single `command:` line.

- [PASS] C11 — `! grep -qE '\-\-port [0-9]+' scripts/start.sh && echo OK` → `OK`
  No hardcoded port number.

- [PASS] C12 — `python3 -c "import yaml; d=yaml.safe_load(open('app.yaml')); assert 'env' not in d, ...; print('OK')"` → `OK`
  No `env` block in app.yaml.

### Standing criteria

- [FAIL] S1 — `bash scripts/verify.sh` exits 1 (not 0).
  59 contract tests fail; all are in `tests/contract/contract-backup/`. These
  failures are pre-existing (present without S6's files) and caused by wrong
  relative paths in the backup directory's test files. The gate as written
  picks up this subdirectory. The gate must exit 0 for S1 to pass.
  → What must change: either `tests/contract/contract-backup/` must be removed
  or excluded from the Playwright testDir, OR a `.gitignore` / Playwright
  `testIgnore` pattern must exclude it so the gate exits 0.
  Note: Fixing this requires touching `tests/contract/playwright.config.ts`
  or removing `tests/contract/contract-backup/`. `scripts/verify.sh` is
  Scope: Out for S6, but `playwright.config.ts` is not explicitly in Scope: Out
  — however, fixing it would be a change to the test infrastructure, not to the
  S6 deliverables. This is a pre-existing defect that blocks the gate.

- [PASS] S2 — Not applicable (no database changes).

- [PASS] S3 — Not applicable (no database changes).

- [PASS] S4 — Not applicable (no database changes).

- [PASS] S5 — Not applicable (no new API routes).

- [PASS] S6 (standing) — No `/__test__/*` routes added; unaffected.

- [PASS] S7 — `git diff | grep -nE '# type: ignore|: any|as any|ts-expect-error'` → empty.
  No type escape hatches introduced.

- [PASS] S8 — `git diff --name-only` → empty (all S6 changes are untracked new files).
  New files `app.yaml`, `scripts/start.sh`, and `specs/S6.md` are all within
  the spec's Scope: In list. No tracked files were modified outside scope.

- [PASS] S9 — `git diff --name-only HEAD -- tests/contract/` → empty.
  Contract test for S6 (`tests/contract/S6.spec.ts`) is untracked (new file,
  written at stage 1 and frozen). No existing contract test file was modified.

- [PASS] S10 — `git diff | grep -nEi 'postgres://|password\s*=|api[_-]?key'` → empty.
  No secrets in the diff.

## Notes

All 12 S6-specific criteria pass cleanly. The sole blocking issue is S1: the
full gate fails due to pre-existing `tests/contract/contract-backup/` test
failures that have nothing to do with S6's deliverables. The `contract-backup/`
directory appears to be a copy of older contract tests left in the workspace but
never committed; the Playwright runner discovers them under its `testDir: '.'`
scan. The fix is to add a `testIgnore` entry in `tests/contract/playwright.config.ts`
to exclude `**/contract-backup/**`, or to delete the directory.

S6's implementation (`app.yaml` and `scripts/start.sh`) is correct per every
rubric criterion. This is a one-line fix to the gate configuration.

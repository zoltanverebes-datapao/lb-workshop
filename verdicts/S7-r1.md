# Verdict: S7 round 1
VERDICT: FAIL

## Gate
scripts/verify.sh — stage reached: contract, exit code: 1

Backend lint, type-check, and tests pass. Frontend types, lint, tests, and build pass. The contract stage exits non-zero: 59 tests fail, 21 pass. All 12 S6.spec.ts tests pass. All 59 failures are in `tests/contract/contract-backup/` — a pre-existing issue where Playwright's `testDir: '.'` discovers the backup directory's broken tests. This is the same root cause as in S6-r1. The gate is unconditional: exit code 1 is a failure regardless of cause.

There are no contract tests for S7 (the spec notes this). S9 applies trivially and passes.

## Per-criterion

### Spec-specific criteria

- [PASS] C1 — `grep -qE 'PGHOST.*PGPORT.*PGDATABASE|PGUSER.*PGHOST.*PGPORT' scripts/start.sh && grep -q 'export DATABASE_URL' scripts/start.sh && echo OK` → `OK`
  The script constructs DATABASE_URL from PG* vars and exports it.

- [PASS] C2 — `grep -qE 'if.*DATABASE_URL|:-|-.DATABASE_URL' scripts/start.sh && echo OK` → `OK`
  Guard: `if [ -z "${DATABASE_URL}" ]` prevents overwriting an existing value.

- [PASS] C3 — `grep -qE 'postgresql://.*PGUSER.*@.*PGHOST.*:.*PGPORT.*/.*PGDATABASE.*sslmode=.*PGSSLMODE' scripts/start.sh && echo OK` → `OK`
  URL is: `postgresql://${PGUSER}@${PGHOST}:${PGPORT}/${PGDATABASE}?sslmode=${PGSSLMODE}`

- [PASS] C4 — `! grep -qiE 'PGPASSWORD|:.*password.*@' scripts/start.sh && echo OK` → `OK`
  No password component in the connection string.

- [PASS] C5 — `grep -qE '\-\-host\s+(0\.0\.0\.0|"0\.0\.0\.0")' scripts/start.sh && echo OK` → `OK`
  Line is `--host 0.0.0.0`.

- [PASS] C6 — `! grep -qE '\-\-host.*PGHOST' scripts/start.sh && echo OK` → `OK`
  The literal string PGHOST is not present in the --host flag.

- [PASS] C7 — `grep -qE '\-\-port.*DATABRICKS_APP_PORT' scripts/start.sh && echo OK` → `OK`
  Line is `--port "$DATABRICKS_APP_PORT"`.

- [PASS] C8 — `! grep -qE '\-\-port.*PGPORT' scripts/start.sh && echo OK` → `OK`
  PGPORT does not appear in the --port flag.

- [PASS] C9 — `grep -qE 'exec .+uvicorn' scripts/start.sh && echo OK` → `OK`
  Final line: `exec uv run uvicorn app.main:app --host 0.0.0.0 --port "$DATABRICKS_APP_PORT"`

- [PASS] C10 — `grep -q 'set -e' scripts/start.sh && echo OK` → `OK`
  First non-shebang line is `set -e`.

- [PASS] C11 — `grep -qE 'npm (install|ci)' scripts/start.sh && grep -q 'npm run build' scripts/start.sh && grep -q 'uv sync' scripts/start.sh && echo OK` → `OK`
  All three steps (npm install, npm run build, uv sync) are present.

- [FAIL] C12 — `bash scripts/verify.sh` exits 1 (not 0).
  59 contract-backup tests fail. All S6.spec.ts tests pass. The gate is unconditional.
  → What must change: `tests/contract/contract-backup/` must be deleted or excluded via a `testIgnore` pattern in `playwright.config.ts`.

### Standing criteria

- [FAIL] S1 — `bash scripts/verify.sh` exits 1.
  Same failure as C12. Pre-existing `contract-backup/` test failures cause the exit code.
  First failing assertion (representative): `expect(fs.existsSync(pkgPath), "/Users/.../tests/frontend/package.json must exist").toBe(true)` → received false.
  → Fix: remove or exclude `tests/contract/contract-backup/`.

- [PASS] S2 — Not applicable. No database changes in this diff.

- [PASS] S3 — Not applicable. No migrations added or changed.

- [PASS] S4 — Not applicable. No migration reversibility concern.

- [PASS] S5 — Not applicable. No new API routes added.

- [PASS] S6 (standing) — No `/__test__/*` routes touched. Unaffected.

- [PASS] S7 (standing) — `git diff HEAD -- scripts/start.sh | grep -nE '# type: ignore|: any|as any|ts-expect-error'` → empty. No type escape hatches introduced.

- [FAIL] S8 — `git diff HEAD --name-only` → `scripts/start.sh` and `specs/S6.md`.
  `specs/S6.md` is not in S7's Scope: In list (`scripts/start.sh` only) and is not in the rubric-base.md exception list (`api/alembic/versions/**`, `api/uv.lock`, `frontend/package-lock.json`, `frontend/src/api/types.ts`).
  → What must change: The generator must not modify `specs/S6.md`. Status updates to prior specs are the implementation lead's responsibility, not the generator's.

- [PASS] S9 — `git diff 42b6345 -- tests/contract/` → empty. No S7 contract tests exist; the item adds none. Contract tests are frozen.

- [PASS] S10 — `git diff HEAD -- scripts/start.sh | grep -nEi 'postgres://|password\s*=|api[_-]?key'` → empty. No secrets or connection strings committed.

## Notes

The `scripts/start.sh` implementation is correct in every substantive respect. All 11 logic criteria (C1–C11) pass cleanly. The two failures are structural:

1. **C12 / S1**: `verify.sh` exits non-zero due to the same pre-existing `tests/contract/contract-backup/` breakage that caused S6-r1 to FAIL. The fix is a one-line `testIgnore` in `playwright.config.ts` (which is Scope: Out for S7 per the spec) or deletion of the directory. This needs to be fixed before S7 can pass.

2. **S8**: The generator modified `specs/S6.md` (changed status from `IN PROGRESS` to `IMPLEMENTED`). That file is outside S7's scope. The diff must only contain `scripts/start.sh`.

To unblock: fix the `contract-backup` gate issue (either in a separate item or by deleting/ignoring the directory), and revert `specs/S6.md` from the working tree before the next generator run, or instruct the generator not to touch it.
